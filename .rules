- Use nix flakes
- Add (but don't commit) any new files to git before trying to build
- To build a package, run `nix build .#<package_name>`
- When a hash is not yet known, use an empty string, then `nix build .#<package_name>` to determine the actual hash
